# Methods

The compute toolkit mapped to tasks: biopython (variant/PAM/restriction reasoning, translation, reverse complement), seqtk (FASTA/FASTQ handling), viennarna (guide/retron folding), and numpy/scipy/pandas for the off-target scan, scoring, and candidate tables.
