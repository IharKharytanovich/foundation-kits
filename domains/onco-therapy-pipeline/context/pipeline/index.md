# The Pipeline Map

The stages of the personalized therapy loop and how they depend on one another, represented as a dependency graph. The map exists to locate the binding constraint — the stage that actually sets the whole loop's throughput.
